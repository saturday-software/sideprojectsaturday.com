import { subscribe } from "./subscribe";
import { events } from "./events";
import { admin } from "./admin";
import { space } from "./space";

export const server = { subscribe, events, admin, space };
